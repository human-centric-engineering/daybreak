# Upstream asks — Daybreak → Sunrise

Fork-first seams Daybreak built ahead of (or composing with) a Sunrise seam that
hasn't landed yet. On every Sunrise pull ([`CUSTOMIZATION.md` §9](../../CUSTOMIZATION.md)),
check each **open** row: if its upstream issue has landed, **delegate to the
upstream resolver and delete the fork shim**, then close the row. This is the
"delegate when it lands" trigger that would otherwise be lost in feature-plan prose.

**This is not a boundary-breach log.** A _breach_ is editing a Sunrise-owned file
because no seam exists (the [`CLAUDE.md`](../../CLAUDE.md) banner's "keep the edit
minimal and add a follow-up" case) — those get a `keep-mine` follow-up, not a row
here. This ledger is the _sanctioned_ fork-first case: the code is clean
framework-tier, but its final home is an upstream seam. See
[`planning/planning-retro.md` B5/B7](./planning/planning-retro.md) for the
fork-first-informs-upstream working model this indexes, and the feature plans /
[`plan.md` Work-completed log](./planning/plan.md) for the narrative each row
summarises.

## The ledger

| Daybreak seam                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Composes with                                                                                                                                                                                                                                                                                                                                                                     | Feature                                                       | Delegate-when-it-lands action                                                                                                                                                                                                                                                                                                                                                                        | Status                                                                                                                                                                                                                                                                                                            |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`lib/framework/shared/access.ts`](../../lib/framework/shared/access.ts) — `canRead` / `subjectScope`                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Sunrise [#367](https://github.com/human-centric-engineering/sunrise/issues/367) (intra-tenant ownership scope) · [#366](https://github.com/human-centric-engineering/sunrise/issues/366) (org-admin / tier axis, carried via `scope.tier`)                                                                                                                                        | [f-journey-state (09)](./planning/f-journey-state.md)         | Replace the self-read + explicit admin-support body with a call to the upstream ownership resolver, passing `scope`; drop the fork-local `JourneyViewer` / `AccessScope` types if the resolver supplies them; keep the `canRead` ⇔ `subjectScope` parity test.                                                                                                                                       | **filed** — fork-note on [#367](https://github.com/human-centric-engineering/sunrise/issues/367#issuecomment-4885272729) (+ tier cross-ref on [#366](https://github.com/human-centric-engineering/sunrise/issues/366#issuecomment-4885273235)); awaiting the upstream resolver                                    |
| [`lib/framework/modules/capabilities/namespace.ts`](../../lib/framework/modules/capabilities/namespace.ts) — `namespaceModuleCapability` (the `NamespacedModuleCapability` wrapper: slug/name override + scope refusal + the PII-contract **re-assertion**)                                                                                                                                                                                                                                                                                                | Sunrise [#398](https://github.com/human-centric-engineering/sunrise/issues/398) — `register(cap, { slug?, guard? })`: a handler-slug override + a generic pre-execute guard, with the PII `redactProvenance` guard run against the real (unwrapped) capability                                                                                                                    | [f-module-bindings (07)](./planning/f-module-bindings.md)     | Delete the `NamespacedModuleCapability` wrapper, its dummy `schema`, and the `inner.processesPii && inner.redactProvenance === BaseCapability.prototype.redactProvenance` re-assertion; register the inner capability directly via `register(inner, { slug: namespacedSlug, guard: scopeGuard })`, moving `isInModuleScope` into the `guard`. Keep the `slug === functionDefinition.name` invariant. | **filed** — Sunrise [#398](https://github.com/human-centric-engineering/sunrise/issues/398); awaiting the seam. Fork wrapper is correct + tested meanwhile (the dispatcher's own PII guard is inert for wrapped caps, so the re-assertion is load-bearing until this lands — do not remove it before delegating). |
| [`lib/orchestration/knowledge/agent-access-contributors.ts`](../../lib/orchestration/knowledge/agent-access-contributors.ts) + the contributor union in [`resolveAgentDocumentAccess.ts`](../../lib/orchestration/knowledge/resolveAgentDocumentAccess.ts) — a **fork-carried core seam** `registerAgentAccessContributor` (the boot-seam #385 pattern: the fork adds the generic seam _in a core file_, not a framework-tier shim). Consumed by [`lib/framework/modules/knowledge/contributor.ts`](../../lib/framework/modules/knowledge/contributor.ts). | Sunrise [#403](https://github.com/human-centric-engineering/sunrise/issues/403) — adopt the generic `registerAgentAccessContributor` seam (keyed registry + the restricted-branch union, contributors only widen, throwing-contributor — async _or_ sync — ignored) into core, so any extension can contribute an agent's knowledge access at resolve time without materialising. | [f-module-bindings (07)](./planning/f-module-bindings.md) t-4 | On the Sunrise pull that lands the seam: **drop the fork-carried edits** to `resolveAgentDocumentAccess` + `agent-access-contributors.ts` in favour of upstream's (reconcile the signature if it differs); **keep** the framework-owned `resolveModuleKnowledgeForAgent` + its `initFramework()` registration and the contributor-union tests.                                                       | **filed** — Sunrise [#403](https://github.com/human-centric-engineering/sunrise/issues/403); awaiting the seam. Enforcement is LIVE in the fork now (the core edit is carried, minimal + generic, empty-registry = prior behaviour). Boundary CI green (no framework vocab, no core→framework import).            |
| [`lib/orchestration/evaluations/parse-structured.ts`](../../lib/orchestration/evaluations/parse-structured.ts) — `runStructuredCompletion`, imported cross-domain by [`lib/framework/data-slots/capabilities/extract.ts`](../../lib/framework/data-slots/capabilities/extract.ts)                                                                                                                                                                                                                                                                          | Sunrise (issue to file) — **relocate** the runner out of `evaluations/` to a neutral `lib/orchestration/llm/` home (its only `evaluations` tie is the OTEL `phase` span tag) and **widen** the `phase` union (`'summary' \| 'scoring'`) to an open string, so non-evaluation callers (slot capture) aren't forced through an eval-shaped API                                      | [f-slot-capture (10)](./planning/f-slot-capture.md)           | Update the `extract.ts` import path to the relocated module and pass a slot-capture-appropriate `phase` tag (e.g. `'slot-extraction'`) instead of omitting it. No behaviour change — the call shape is already correct.                                                                                                                                                                              | **identified — issue to file.** Meanwhile `extract.ts` imports it at its current `evaluations/` path (a framework→core **public** import, no core edit) and omits `phase` (spans tag the default `evaluation`); correct + tested.                                                                                 |
| [`lib/orchestration/capabilities/types.ts`](../../lib/orchestration/capabilities/types.ts) — `CapabilityContext`; consumed by [`lib/framework/data-slots/capabilities/exposure.ts`](../../lib/framework/data-slots/capabilities/exposure.ts) via a per-execute re-read of `AiAgentCapability.customConfig`                                                                                                                                                                                                                                                 | Sunrise (issue to file) — surface the resolved binding's `customConfig` (+ `isEnabled`) onto `CapabilityContext` alongside the existing `scope` carrier, so a capability need not re-query its own grant at execute time                                                                                                                                                          | [f-slot-capture (10)](./planning/f-slot-capture.md)           | Delete `loadExposureConfig`'s `prisma.aiAgentCapability.findFirst` and read `context.customConfig` instead (keep the Zod parse + fail-closed + `facetAllows`); the one indexed lookup per capture disappears.                                                                                                                                                                                        | **identified — issue to file.** Meanwhile each capability re-reads its own binding (one lookup served by `@@unique([agentId, capabilityId])`) — zero core edit, mirrors `f-module-bindings`.                                                                                                                      |
| [`lib/orchestration/chat/context-builder.ts`](../../lib/orchestration/chat/context-builder.ts) — a **fork-carried core seam**: `buildContext`/`ContextContributor` widened to pass a generic `ContextRequest { userId? }` + a **user-aware cache key** (the boot-seam #385 / #403 pattern — the fork adds the generic widening _in a core file_, not a framework shim). Consumed by [`lib/framework/modules/context.ts`](../../lib/framework/modules/context.ts) `loadModuleContext`.                                                                      | Sunrise (issue to file) — adopt the generic context-contributor widening into core: a contributor receives per-request inputs (`userId`, later `conversationId`) and the result cache is scoped per `(type, id, userId)`, so an extension can return **per-user** prompt context without cross-user leak. Empty `userId` = today's shared behaviour exactly.                      | [f-guidance (12)](./planning/f-guidance.md) t-4b              | On the Sunrise pull that lands the widened seam: **drop the fork-carried `context-builder.ts` edits** (and the `buildContext`/`invalidateContext` call-site `userId` args in `streaming-handler.ts`) in favour of upstream's (reconcile the signature if it differs); **keep** the framework-owned `loadModuleContext` per-user injection + its tests.                                               | **carried — issue to file.** The core widening is LIVE in the fork now (minimal + generic, no framework vocab; **empty `userId` = prior behaviour**; boundary CI green). t-4b injects the user's fresh slot values for the module's open slots on top of t-4's user-agnostic module context.                      |

## Adding a row

When a feature builds a seam whose final home is an upstream Sunrise seam:

1. Add a row here (seam file, upstream issue(s), owning feature, the concrete
   delegate action, status).
2. File / update the fork-perspective note on the existing Sunrise issue as the
   feature's own Done-when deliverable (retro B7) — a **new** issue only if none
   already tracks the seam.
3. Close the row on the Sunrise pull that lands the seam, once the shim is deleted
   and callers delegate.
