/**
 * Data-slots domain — what the system learns about the user: insert-only
 * versioned slot values with confidence, source type, and per-version
 * provenance, plus silent capture capabilities.
 *
 * Empty skeleton (f-bootstrap t-1). Populated from `f-slots` / `f-slot-capture`
 * onward — see `.context/framework/planning/plan.md` (features 05, 10) and the
 * spec §6.
 */
export {};
